<p>
	<strong>Title:</strong>
	<%= @test.title %>
</p>

<p>
	<strong>Level:</strong>
	<%= @test.level %>
</p>

<h2>Questions:</h2>

<%= link_to 'New Question', new_test_question_path(@test) %>

<p>
	<strong>Quantity of questions:</strong>
	<%= @test.questions.count %>
</p>

<%= render 'shared/questions', test: @test %>

<%= link_to 'Edit', edit_admin_test_path(@test) %>
<%= link_to 'Back', admin_tests_path %>